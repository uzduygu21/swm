import { BoldKeywordPipe } from './bold-keyword.pipe';

describe('BoldKeywordPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldKeywordPipe();
    expect(pipe).toBeTruthy();
  });
});
